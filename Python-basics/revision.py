import sys
import random
print(sys.version)   # this will print the version of python that you are using. It is important to know the version of python you are using because some features may not be available in older versions of python.
print(random.randrange(1,10))  # this will print a random number between 1 and 10. The randrange() function is used to generate a random number within a specified range. In this case, it will generate a random number between 1 and 10 (inclusive of 1 but exclusive of 10).
x=float(1)
