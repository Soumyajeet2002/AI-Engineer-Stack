#22-04-2026
# Python Output Number
## Print NUmbers 
- we can print the number using `print()`. Function to display number.
```
print(3)
print(358)
print(50000)
```

- we can also maths under the `print()` function 
```
print(3 - 2)
print(100 + 90)
```
- we can even add both TEXT and NUMBERS in out print function
```
print("Hi this is Soumyajeet" , "I am " , 24 )
```

# Python Variables 
- Variables are containers for storing data / values.

## Creating Variables 
- Python has no commands for declaring a variable.
- variable is created the moment you first assign a value to it.
```
x = 5 -> assigning numaric value to X
y = "NAME" -> assigning String valuee to Y
print(x)
print(y)
```

- variable do not nbeed to be declared with any particular type , and can even change after they have been assigned.

```
x = 50
x = "JEET"

print(X)  -> this prints the latest assigned value. to X
```
## Variable Casting
- if we wnat to specify the data type of any variable we can use casting
```
x = str(3)   # x = will be '3' -> string
y = int(3)   # x = will be 3 -> integer
z = float(3) # X will be 3.0 -> float value
```

## Get the type 
```
x = 5
y = "john"
print(type(x))
print(type(y))
-> this prints the type of the variable 
```

## Single or Double Quptes
- String varibale can be declared both using single or double Quotes.
```
They both work the same 

X = 'Hello' -> They both define the sam condition  
Y = "Hello" -> They both define the sam condition
```


## Case Sensitive
- Variable names are case Sensitive
-- For example 
```
a = "Soumyajeet"
A = 12

print(a)
print(A)

-> in this 2 differnet variables are being created and both are being asigned with different values.
```

